# Orbit Agent Configuration
# ===========================

# Required: OpenAI API Key (used for planning + vision)
# Never commit your real key. Keep it only in `.env` (gitignored).
OPENAI_API_KEY=sk-...

# Optional: Orbit Uplink (Mobile Access via Telegram)
# 1. Message @BotFather on Telegram
# 2. Send /newbot and follow instructions
# 3. Paste your token below
TELEGRAM_BOT_TOKEN=

# Security: Comma-separated Telegram user IDs allowed to control your PC
# Find your ID by messaging @userinfobot on Telegram
ORBIT_UPLINK_USERS=

# Optional: Runtime auth PIN (if you want to authorize new users via /auth command)
ORBIT_UPLINK_PIN=

# Uplink behavior (optional)
# - Default is OFF: no automatic "proof" screenshots after tasks
# - Set to 1/true/yes/on to enable auto screenshots
ORBIT_UPLINK_SCREENSHOTS=0

# Uplink autonomy (optional)
# If a step fails, Uplink can ask the planner for a recovery plan and retry.
# Keep this small to avoid loops.
ORBIT_UPLINK_REPLAN_MAX=1

# Desktop input backend (optional)
# - 'auto' (default): uses pydirectinput if installed, else PyAutoGUI
# - 'direct': force pydirectinput (best for DirectX/fullscreen games)
# - 'pyautogui': force PyAutoGUI
#
# If you set 'direct', install dependency: pip install pydirectinput
ORBIT_DESKTOP_INPUT_BACKEND=auto

# Safety (optional)
# Block obviously destructive shell commands by default.
# Set to 1 only if you REALLY want to allow dangerous commands (not recommended).
ORBIT_ALLOW_DANGEROUS_COMMANDS=0

# Self-extension (optional): allow Orbit to create and hot-load new skills at runtime
# - Creation: allows writing a new skill file under ./data/skills/
# - Autoload: immediately imports/registers the new skill without restarting the agent
ORBIT_ENABLE_SKILL_CREATE=0
ORBIT_ENABLE_SKILL_AUTOLOAD=0

# Proactive scheduler (optional)
# Create reminders from Telegram:
# - /remind <minutes> <goal...>
# - /daily HH:MM <goal...>
# - /jobs, /cancel <id>
# - /heartbeat <minutes> | /heartbeat off
